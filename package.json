{
  "scripts": {
    "preinstall": "npx -y only-allow pnpm",
    "lint": "eslint \"{src,apps,libs,test,packages}/**/*.ts\" --fix",
    "add": "changeset add",
    "version": "changeset version",
    "publish": "changeset publish"
  },
  "keywords": [],
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.25.2",
    "@mmc-cloud/eslint-config-ts": "^1.0.1",
    "eslint": "^8.29.0",
    "tsup": "^6.5.0",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "@mmc-cloud/web-lib": "^0.0.4"
  }
}
